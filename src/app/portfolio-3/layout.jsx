export const metadata = {
    title: "Portfolio 2",
    description: "Portfolio 2",
  };

export default function Portfolio2Layout({ children }) {
  return (  
    <div>
        {children}
    </div>
  );
}
