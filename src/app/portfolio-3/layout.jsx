export const metadata = {
    title: "Portfolio 3",
    description: "Portfolio 3",
  };

export default function Portfolio3Layout({ children }) {
  return (  
    <div>
        {children}
    </div>
  );
}
