export const metadata = {
    title: "Portfolio 1",
    description: "Portfolio 1",
  };

export default function Portfolio1Layout({ children }) {
  return (  
    <div>
        {children}
    </div>
  );
}
